{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "App-owned library commands only. No generic filesystem, SQL, shell, or HTTP plugin access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
